# 🚗 CarWash - Агрегатор автомоек с бронированием

Telegram-бот и Mini App для бронирования автомоек с предоплатой 50%.

## 📁 Структура проекта

```
CarWash/
├── src/
│   ├── bot/                    # Telegram бот
│   │   ├── handlers/           # Обработчики команд
│   │   │   ├── user.py         # Команды пользователя
│   │   │   ├── booking.py      # Процесс бронирования
│   │   │   └── admin_wash.py   # Админ автомойки
│   │   ├── keyboards/          # Клавиатуры
│   │   ├── states.py           # FSM состояния
│   │   └── main.py             # Точка входа бота
│   │
│   ├── routers/v1/             # API эндпоинты
│   │   ├── carwash.py          # CRUD автомоек
│   │   ├── booking.py          # Бронирования
│   │   ├── payment.py          # Платежи
│   │   ├── user.py             # Пользователи
│   │   └── washtype.py         # Типы мойки
│   │
│   ├── models/                 # SQLAlchemy модели
│   ├── schemas/                # Pydantic схемы
│   ├── core/                   # Конфигурация, БД
│   └── application.py          # FastAPI приложение
│
├── webapp/                     # Telegram Mini App (React)
│   ├── src/
│   │   ├── pages/              # Страницы
│   │   ├── components/         # UI компоненты
│   │   ├── api/                # API клиент
│   │   ├── store/              # Zustand store
│   │   └── App.jsx
│   └── package.json
│
├── migrations/                 # Alembic миграции
├── docker-compose.yml
└── requirements.txt
```

## 🚀 Быстрый старт

### 1. Переменные окружения

Создайте файл `.env`:

```env
# Database
postgres_url=postgresql+asyncpg://user:password@localhost:5432/carwash

# Telegram Bot
bot_token=YOUR_BOT_TOKEN
admin_id=YOUR_TELEGRAM_ID

# Payment (YooKassa)
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key

# Mini App
WEBAPP_URL=https://your-domain.com/webapp
```

### 2. Запуск с Docker

```bash
docker-compose up -d
```

### 3. Запуск вручную

```bash
# 1. Установите зависимости
pip install -r requirements.txt
cd webapp
npm install
cd ..

# 2. Запустите миграции базы данных
alembic upgrade head

# 3. Запустите Backend API (в первом терминале)
uvicorn src.application:get_app --host 0.0.0.0 --port 8000 --factory --reload

# 4. Запустите Telegram-бота (во втором терминале)
#    Убедитесь, что в .env установлен BOT_MODE=polling
# Он запускаеться вместе с FASTAPI в пункте № 3

# 5. Запустите Mini App (в третьем терминале)
cd webapp
npm run dev
```

## 📱 API Endpoints

### Автомойки
- `GET /api/v1/carwashes` - Список моек (с геолокацией)
- `GET /api/v1/carwashes/{id}` - Детали мойки
- `GET /api/v1/carwashes/{id}/slots` - Свободные слоты

### Бронирования
- `POST /api/v1/bookings/create` - Создать бронь
- `GET /api/v1/bookings/my?phone=...` - Мои брони
- `GET /api/v1/bookings/{id}` - Детали брони
- `POST /api/v1/bookings/{id}/cancel` - Отменить

### Платежи
- `POST /api/v1/payments/create` - Создать платёж
- `POST /api/v1/payments/webhook` - Webhook от ЮKassa

### Пользователи
- `POST /api/v1/users/telegram/auth` - Авторизация через TG
- `GET /api/v1/users/me` - Мой профиль

## 🤖 Telegram Bot команды

- `/start` - Главное меню
- Кнопка "Забронировать" - Открывает Mini App
- Кнопка "Мои брони" - Список бронирований
- Кнопка "Профиль" - Профиль пользователя

### Админ-панель (для admin_id)
- "Добавить мойку" - Регистрация новой мойки
- "Удалить мойку" - Удаление мойки
- "Статистика" - Общая статистика

## 💳 Интеграция платежей

Проект готов к интеграции с:
- ЮKassa
- Т-Касса
- Stripe

Предоплата 50% при бронировании, остаток на месте.

## 📊 Основные сущности

- **User** - Пользователь (Telegram ID)
- **CarWash** - Автомойка
- **WashBay** - Бокс мойки
- **WashType** - Тип мойки (Стандарт, Премиум и т.д.)
- **TimeSlot** - Временной слот
- **Booking** - Бронирование

## 🔐 Безопасность

- Валидация Telegram Init Data
- Проверка подписи webhook'ов
- Rate limiting
- CORS для Mini App

## 📝 TODO

- [ ] Реальная интеграция с ЮKassa
- [ ] Push-уведомления
- [ ] Система отзывов и рейтингов
- [ ] Панель системного администратора (Web)
- [ ] Геокодинг адресов
- [ ] Аналитика и отчёты

## 📄 Лицензия

MIT
